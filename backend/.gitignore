# Dependencias (Render las instalará por su cuenta)
node_modules/

# Variables de entorno (¡SÚPER CRÍTICO! Nunca subas esto)
.env
.env.local
.env.*.local

# Logs y archivos de depuración
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema operativo (macOS / Windows)
.DS_Store
Thumbs.db

# Carpetas de builds o compilación (Si llegaras a usar TypeScript)
dist/
build/

# Cobertura de pruebas (Si usas Jest, Mocha, etc.)
coverage/